/**
 * Interface for Hero Data
 * @interface
 * @property {string} heroImage - The URL of the hero image
 * @property {string} heroSubheading - The subheading text for the hero section
 * @property {string} heroHeading - The heading text for the hero section
 */
export interface HeroData {
    heroImage: string;
    heroSubheading: string;
    heroHeading: string;
}